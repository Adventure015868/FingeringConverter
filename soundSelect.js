// 運指を取り出すための変数
let i = 0;

// 運指を取り出す関数
let cMusic;
function getC() {
    if (musicMIname === "チューバ B♭管 4本バルブ") {
        cMusic = Tuba_C[i]
    };
    if (musicMIname === "ユーフォニアム") {
        cMusic = Euph_C[i]
    };
    if (musicMIname === "トロンボーン") {
        cMusic = Tb_C[i]
    };
    if (musicMIname === "トランペット") {
        cMusic = Tp_C[i]
    };
    if (musicMIname === "ホルン Fシングル") {
        cMusic = HrA_C[i]
    };
    if (musicMIname === "ホルン B♭シングル") {
        cMusic = HrB_C[i]
    };
    if (musicMIname === "ホルン F/B♭フルダブル") {
        cMusic = HrC_C[i]
    };
};

let descisMusic;
function getDesCis() {
    if (musicMIname === "チューバ B♭管 4本バルブ") {
        descisMusic = Tuba_DesCis[i]
    };
    if (musicMIname === "ユーフォニアム") {
        descisMusic = Euph_DesCis[i]
    };
    if (musicMIname === "トロンボーン") {
        descisMusic = Tb_DesCis[i]
    };
    if (musicMIname === "トランペット") {
        descisMusic = Tp_DesCis[i]
    };
    if (musicMIname === "ホルン Fシングル") {
        descisMusic = HrA_DEsCis[i]
    };
    if (musicMIname === "ホルン B♭シングル") {
        descisMusic = HrB_DesCis[i]
    };
    if (musicMIname === "ホルン F/B♭フルダブル") {
        descisMusic = HrC_DesCis[i]
    };
};

let dMusic;
function getD() {
    if (musicMIname === "チューバ B♭管 4本バルブ") {
        dMusic = Tuba_D[i]
    };
    if (musicMIname === "ユーフォニアム") {
        dMusic = Euph_D[i]
    };
    if (musicMIname === "トロンボーン") {
        dMusic = Tb_D[i]
    };
    if (musicMIname === "トランペット") {
        dMusic = Tp_D[i]
    };
    if (musicMIname === "ホルン Fシングル") {
        dMusic = HrA_D[i]
    };
    if (musicMIname === "ホルン B♭シングル") {
        dMusic = HrB_D[i]
    };
    if (musicMIname === "ホルン F/B♭フルダブル") {
        dMusic = HrC_D[i]
    };
};

let esdisMusic;
function getEsDis() {
    if (musicMIname === "チューバ B♭管 4本バルブ") {
        esdisMusic = Tuba_EsDis[i]
    };
    if (musicMIname === "ユーフォニアム") {
        esdisMusic = Euph_EsDis[i]
    };
    if (musicMIname === "トロンボーン") {
        esdisMusic = Tb_EsDis[i]
    };
    if (musicMIname === "トランペット") {
        esdisMusic = Tp_EsDis[i]
    };
    if (musicMIname === "ホルン Fシングル") {
        esdisMusic = HrA_EsDis[i]
    };
    if (musicMIname === "ホルン B♭シングル") {
        esdisMusic = HrB_EsDis[i]
    };
    if (musicMIname === "ホルン F/B♭フルダブル") {
        esdisMusic = HrC_EsDis[i]
    };
};

let eMusic;
function getE() {
    if (musicMIname === "チューバ B♭管 4本バルブ") {
        eMusic = Tuba_E[i]
    };
    if (musicMIname === "ユーフォニアム") {
        eMusic = Euph_E[i]
    };
    if (musicMIname === "トロンボーン") {
        eMusic = Tb_E[i]
    };
    if (musicMIname === "トランペット") {
        eMusic = Tp_E[i]
    };
    if (musicMIname === "ホルン Fシングル") {
        eMusic = HrA_E[i]
    };
    if (musicMIname === "ホルン B♭シングル") {
        eMusic = HrB_E[i]
    };
    if (musicMIname === "ホルン F/B♭フルダブル") {
        eMusic = HrC_E[i]
    };
};

let fMusic;
function getF() {
    if (musicMIname === "チューバ B♭管 4本バルブ") {
        fMusic = Tuba_F[i]
    };
    if (musicMIname === "ユーフォニアム") {
        fMusic = Euph_F[i]
    };
    if (musicMIname === "トロンボーン") {
        fMusic = Tb_F[i]
    };
    if (musicMIname === "トランペット") {
        fMusic = Tp_F[i]
    };
    if (musicMIname === "ホルン Fシングル") {
        fMusic = HrA_F[i]
    };
    if (musicMIname === "ホルン B♭シングル") {
        fMusic = HrB_F[i]
    };
    if (musicMIname === "ホルン F/B♭フルダブル") {
        fMusic = HrC_F[i]
    };
};

let gesfisMusic;
function getGesFis() {
    if (musicMIname === "チューバ B♭管 4本バルブ") {
        gesfisMusic = Tuba_GesFis[i]
    };
    if (musicMIname === "ユーフォニアム") {
        gesfisMusic = Euph_GesFis[i]
    };
    if (musicMIname === "トロンボーン") {
        gesfisMusic = Tb_GesFis[i]
    };
    if (musicMIname === "トランペット") {
        gesfisMusic = Tp_GesFis[i]
    };
    if (musicMIname === "ホルン Fシングル") {
        gesfisMusic = HrA_GesFis[i]
    };
    if (musicMIname === "ホルン B♭シングル") {
        gesfisMusic = HrB_GesFis[i]
    };
    if (musicMIname === "ホルン F/B♭フルダブル") {
        gesfisMusic = HrC_GesFis[i]
    };
};

let gMusic;
function getG() {
    if (musicMIname === "チューバ B♭管 4本バルブ") {
        gMusic = Tuba_G[i]
    };
    if (musicMIname === "ユーフォニアム") {
        gMusic = Euph_G[i]
    };
    if (musicMIname === "トロンボーン") {
        gMusic = Tb_G[i]
    };
    if (musicMIname === "トランペット") {
        gMusic = Tp_G[i]
    };
    if (musicMIname === "ホルン Fシングル") {
        gMusic = HrA_G[i]
    };
    if (musicMIname === "ホルン B♭シングル") {
        gMusic = HrB_G[i]
    };
    if (musicMIname === "ホルン F/B♭フルダブル") {
        gMusic = HrC_G[i]
    };
};

let asgisMusic;
function getAsGis() {
    if (musicMIname === "チューバ B♭管 4本バルブ") {
        asgisMusic = is[i]
    };
    if (musicMIname === "ユーフォニアム") {
        asgisMusic = Euph_AsGis[i]
    };
    if (musicMIname === "トロンボーン") {
        asgisMusic = Tb_AsGis[i]
    };
    if (musicMIname === "トランペット") {
        asgisMusic = Tp_AsGis[i]
    };
    if (musicMIname === "ホルン Fシングル") {
        asgisMusic = HrA_AsGis[i]
    };
    if (musicMIname === "ホルン B♭シングル") {
        asgisMusic = HrB_AsGis[i]
    };
    if (musicMIname === "ホルン F/B♭フルダブル") {
        asgisMusic = HrC_AsGis[i]
    };
};

let aMusic;
function getA() {
    if (musicMIname === "チューバ B♭管 4本バルブ") {
        aMusic = Tuba_A[i]
    };
    if (musicMIname === "ユーフォニアム") {
        aMusic = Euph_A[i]
    };
    if (musicMIname === "トロンボーン") {
        aMusic = Tb_A[i]
    };
    if (musicMIname === "トランペット") {
        aMusic = Tp_A[i]
    };
    if (musicMIname === "ホルン Fシングル") {
        aMusic = HrA_A[i]
    };
    if (musicMIname === "ホルン B♭シングル") {
        aMusic = HrB_A[i]
    };
    if (musicMIname === "ホルン F/B♭フルダブル") {
        aMusic = HrC_A[i]
    };
};

let baisMusic;
function getBAis() {
    if (musicMIname === "チューバ B♭管 4本バルブ") {
        baisMusic = Tuba_BAis[i]
    };
    if (musicMIname === "ユーフォニアム") {
        baisMusic = Euph_BAis[i]
    };
    if (musicMIname === "トロンボーン") {
        baisMusic = Tb_BAis[i]
    };
    if (musicMIname === "トランペット") {
        baisMusic = Tp_BAis[i]
    };
    if (musicMIname === "ホルン Fシングル") {
        baisMusic = HrA_BAis[i]
    };
    if (musicMIname === "ホルン B♭シングル") {
        baisMusic = HrB_BAis[i]
    };
    if (musicMIname === "ホルン F/B♭フルダブル") {
        baisMusic = HrC_BAis[i]
    };
};

let hMusic;
function getH() {
    if (musicMIname === "チューバ B♭管 4本バルブ") {
        hMusic = Tuba_H[i]
    };
    if (musicMIname === "ユーフォニアム") {
        hMusic = Euph_H[i]
    };
    if (musicMIname === "トロンボーン") {
        hMusic = Tb_H[i]
    };
    if (musicMIname === "トランペット") {
        hMusic = Tp_H[i]
    };
    if (musicMIname === "ホルン Fシングル") {
        hMusic = HrA_H[i]
    };
    if (musicMIname === "ホルン B♭シングル") {
        hMusic = HrB_H[i]
    };
    if (musicMIname === "ホルン F/B♭フルダブル") {
        hMusic = HrC_H[i]
    };
};